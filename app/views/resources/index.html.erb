<h1>All Resources</h1>

<% @resources.each do |r| %>
<p>
<%= link_to "NEED TO ADD RESOURCE NAME OR SOMETHING HERE", resource_path(r.id)%> -- <%= link_to "Add Review", new_resource_review_path(r.id)%>
</p>
<% end %>