<h1><%= index_review_header %> </h1>


<div class="container">
    <div class="row"> 
        <% @reviews.each do |r| %>
            <%= render partial: 'review_card', locals: {r: r} %>
        <% end %>
    </div>
</div>

<%== pagy_bootstrap_nav(@pagy)%>